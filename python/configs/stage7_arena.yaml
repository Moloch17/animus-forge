# Stage 7 of the curriculum: self-play one-on-one. Two learned agents of random classes and roles fight in each env,
# both played by this policy, so both sides of every fight are training data, across class matchups.
#
# Against itself a policy's score says little about progress (both sides improve together), so evaluation plays the
# other side with the fight baseline (eval.opponent_baseline): the score is the learner's first seat against a fixed
# scripted opponent, and the baseline score is fight against fight on the same seeds. Convergence and the target work
# on that score like any other stage's.

extends: stage6_pvp.yaml
run_name: stage7_arena

# ~4 h at ~14k env steps/s (two seats).
total_env_steps: 30000000

eval:
  baseline: fight
  opponent_baseline: true
  report: [won, dps, died, time_to_kill, damage_taken, cast_seconds_wasted]

# A sweep of the whole curriculum: every stage trains for at most total_env_steps and then moves on, whether or not
# it would have passed. The gates are cleared rather than left to the extended stage -- sections merge key by key, so
# an inherited `metrics` survives unless an empty map replaces it -- which disables the target (TargetConfig.enabled)
# and makes convergence and the budget both advance instead of halting the queue. Restore the gates for a run meant
# to prove a stage rather than to cover ground; they are in git history.
target:
  metrics: {}
  layout_metrics: {}
  role_metrics: {}
  arenas: {}
  difficulties: {}
  min_over_baseline: null
  min_layout_over_baseline: null
  until_passed: false
